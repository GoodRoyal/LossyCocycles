import ProofWidgets.Component.HtmlDisplay
/-! Widgets stub (master plan Part C). Planned: W1 the (norm × defect) grid with anchors;
W2 torus flux + vortex corrections (SVG); W3 plateau curves bound to e2v2 npz numbers.
APIs to be written against the INSTALLED ProofWidgets version, not from memory. -/
